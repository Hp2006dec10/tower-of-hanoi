I updated the application in such a way that the timer (in minutes) is fetched from the database and that will be the timer of reset. 
This is setup as snapshot in case of avoiding to interrupt the users to restart and allow them to play continuously till the new reset time
